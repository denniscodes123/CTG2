using Terraria;
using Terraria.ModLoader;
using Terraria.ID;

public class GemLogic : ModPlayer
{
/*
    public override void PostUpdate()
    {
        int tileX = (int)(Player.position.X / 16);
        int tileY = (int)(Player.position.Y / 16); // Player's current position

        // --- Tile Detection (Grass) ---
        Tile tile = Main.tile[tileX, tileY + 1]; // Tile beneath the player
        if (tile != null && tile.HasTile && tile.TileType == TileID.Grass)
        {
            Main.NewText("You stepped on Grass!", 0, 255, 0);
        }

        // --- Wall Detection (Dirt Wall) ---
        int wallType = wall[tileX, tileY]; // Get the wall type at the player's position
        if (wallType == WallID.Dirt)
        {
            Main.NewText("You are standing in front of a Dirt Wall!", 139, 69, 19); // Brown text
        }
    }
    */
}
